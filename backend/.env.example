# --- OPCIÓN A: Nube (Neon, Render, Supabase) ---
# Pega aquí la cadena de conexión completa de tu base de datos
# DATABASE_URL=postgresql://usuario:password@host/basededatos?sslmode=require

# --- OPCIÓN B: Desarrollo local ---
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=tu_password
DB_DATABASE=postgres
# DB_SSL=true  # Descomentar solo si el servidor requiere SSL
